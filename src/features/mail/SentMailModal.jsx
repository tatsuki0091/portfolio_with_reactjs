import React, { useState } from "react";
import styled from "styled-components";

const SentMailModal = () => {
  const [sentMailModal, setSentMailModal] = useState(false);
  return <></>;
};

export default SentMailModal;
